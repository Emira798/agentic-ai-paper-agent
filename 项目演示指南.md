# 项目演示指南：AI论文助手 CI/CD流程演示

## 🎯 演示目标

向老师完整展示项目的CI/CD自动化流程，包括：
- GitHub Actions 持续集成
- 自动化测试执行
- 代码质量检查
- 腾讯云托管自动部署（Webhook触发）

## 📋 演示前准备

### 1. 环境检查
```bash
# 检查本地环境
python --version
pip --version
docker --version
git --version

# 检查API密钥配置
echo $DASHSCOPE_API_KEY
echo $TAVILY_API_KEY
```

### 2. 项目状态确认
```bash
# 查看当前分支
git status
git branch

# 确保代码是最新的
git pull origin main
```

### 3. 本地测试预运行
```bash
# 快速验证项目功能
python main.py &
sleep 5
curl http://localhost:8000/health
kill %1

# 运行基础测试
python -m pytest tests/test_basic.py -v
```

## 🎬 演示流程

### 第一部分：项目架构介绍 (5分钟)

#### 1.1 项目结构展示
```bash
# 展示项目文件结构
tree -I '__pycache__|*.pyc' --dirsfirst

# 重点文件说明
echo "核心组件："
echo "├── main.py              # FastAPI应用入口"
echo "├── src/                 # 核心业务逻辑"
echo "├── tests/              # 测试套件"
echo "├── .github/workflows/  # CI/CD配置"
echo "└── Dockerfile          # 容器化配置"
```

#### 1.2 技术栈介绍
```
✅ Python + FastAPI          # 后端框架
✅ PostgreSQL               # 数据库
✅ Docker                   # 容器化
✅ GitHub Actions          # CI/CD平台
✅ 通义千问 + Tavily API   # AI能力
```

### 第二部分：CI/CD流程演示 (10分钟)

#### 2.1 本地测试演示
```bash
# 演示完整的测试流程
echo "🔍 开始演示测试流程..."

# 1. 基础测试（免费）
echo "\n1️⃣ 运行基础测试（语法检查、导入测试）"
python -m pytest tests/test_basic.py -v --tb=short

# 2. 模型测试
echo "\n2️⃣ 运行数据库模型测试"
python -m pytest tests/test_models.py -v --tb=short

# 3. API测试演示
echo "\n3️⃣ 运行真实API测试（展示AI功能）"
python -m pytest tests/test_real_api.py -v --tb=short

# 4. 完整测试套件
echo "\n4️⃣ 运行完整测试套件（CI门禁）"
./run_tests.sh
```

#### 2.2 GitHub Actions 演示
```bash
# 展示GitHub Actions配置
echo "📋 展示CI配置文件："
cat .github/workflows/ci.yml

echo "\n🎯 CI流程说明："
echo "1. 代码推送触发CI工作流"
echo "2. 自动设置Python环境"
echo "3. 安装依赖和测试工具"
echo "4. 配置API密钥"
echo "5. 代码质量检查（flake8）"
echo "6. 综合测试执行（门禁检查）"
echo "7. 覆盖率报告生成"
echo "8. Docker镜像构建测试"

echo "\n🌐 Webhook自动触发："
echo "9. GitHub自动通知腾讯云托管"
echo "10. 腾讯云检查CI结果"
echo "11. 只有CI通过才继续部署"
```

#### 2.3 代码质量检查演示
```bash
# 演示flake8代码质量检查
echo "🔍 代码质量检查演示："
python -m flake8 src/ tests/ main.py --count --select=E9,F63,F7,F82 --show-source --statistics

echo "\n✅ 代码质量指标："
echo "- 语法错误检查"
echo "- 代码复杂度分析"
echo "- 代码规范验证"
echo "- PEP8标准遵循"
```

### 第三部分：腾讯云托管自动部署演示 (10分钟)

#### 3.1 腾讯云托管介绍
```
☁️  腾讯云托管（Cloud Run）特性：
✅ 无服务器容器部署
✅ 自动扩缩容
✅ 内置负载均衡
✅ 自动运维管理
✅ Webhook自动触发部署
```

#### 3.2 Webhook配置展示
```bash
# 展示Webhook连接
echo "🔗 Webhook自动连接："
echo "1. 腾讯云托管已配置Webhook"
echo "2. GitHub收到代码推送后自动通知腾讯云"
echo "3. 腾讯云检查CI结果，只有通过才部署"

echo "\n📋 查看Webhook状态："
echo "GitHub仓库 → Settings → Webhooks"
echo "Webhook地址: https://cloud.tencent.com/webhook/xxx"
echo "状态: ✅ 活跃"
```

#### 3.3 Docker配置展示（腾讯云使用）
```bash
# 展示Docker配置（腾讯云托管使用）
echo "🐳 Docker配置（腾讯云托管使用）："
cat Dockerfile

echo "\n📋 腾讯云托管部署说明："
echo "1. 腾讯云自动读取Dockerfile"
echo "2. 自动构建Docker镜像"
echo "3. 自动停止旧容器"
echo "4. 自动启动新容器"
echo "5. 自动健康检查"

echo "\n🚀 完全自动化流程："
echo "git push → CI测试 → Webhook通知 → 自动部署"
```

### 第四部分：实时监控演示 (5分钟)

#### 4.1 GitHub Actions + 腾讯云托管监控
```
🌐 实时演示步骤：
1. 打开 GitHub 仓库 → Actions 标签页
2. 展示当前运行的CI工作流
3. 实时查看测试进度和结果
4. 打开腾讯云托管控制台
5. 展示自动部署状态

⏰ 时间线演示：
0秒: git push完成
10秒: GitHub Actions开始运行
30秒: 腾讯云收到Webhook通知
1分钟: CI测试完成（绿色✅）
2分钟: 腾讯云部署完成
```

#### 4.2 腾讯云托管监控
```
📊 腾讯云托管控制台：
1. 服务状态: 正常/部署中
2. 实例数量: 自动扩缩容
3. CPU/内存使用率
4. 请求量和响应时间
5. 部署历史记录
```

#### 4.3 应用健康检查
```bash
# 演示应用健康检查
echo "🏥 应用健康检查："

# 检查API端点（腾讯云托管域名）
echo "\n1. 健康检查端点："
curl -s https://your-tencent-cloud-run-domain.com/health

echo "\n2. 任务列表接口："
curl -s https://your-tencent-cloud-run-domain.com/api/tasks

echo "\n3. 任务生成测试："
curl -X POST https://your-tencent-cloud-run-domain.com/api/tasks \
  -H "Content-Type: application/json" \
  -d '{"title": "演示测试", "description": "CI/CD演示任务", "paper_type": "research"}'
```

## 🎯 演示亮点展示

### 亮点1：真实API测试
```
✅ 展示真实AI API调用
✅ 成本控制机制
✅ 错误处理和重试
✅ 响应时间监控
```

### 亮点2：腾讯云托管深度集成
```
✅ Webhook自动触发部署
✅ CI门禁检查（只有测试通过才部署）
✅ 零停机滚动更新
✅ 自动扩缩容
✅ 内置监控和日志
```

### 亮点3：生产就绪特性
```
✅ Docker容器化
✅ PostgreSQL数据库
✅ 环境变量管理
✅ 健康检查端点
✅ 腾讯云托管PaaS平台
```

## ⚠️ 常见问题处理

### 问题1：API调用失败
```bash
# 检查API密钥
echo $DASHSCOPE_API_KEY
echo $TAVILY_API_KEY

# 检查网络连接
ping dashscope.aliyuncs.com

# 测试API连接
curl -I https://dashscope.aliyuncs.com/compatible-mode/v1
```

### 问题2：部署失败
```bash
# 检查Docker服务
sudo systemctl status docker

# 检查端口占用
sudo lsof -i :8000

# 查看错误日志
docker-compose logs --tail=50
```

### 问题3：数据库连接问题
```bash
# 检查数据库状态
sudo systemctl status postgresql

# 检查连接配置
echo $DATABASE_URL

# 测试数据库连接
psql "$DATABASE_URL" -c "SELECT 1;"
```

## 📊 演示总结

### 核心优势
```
🎯 完整的CI/CD自动化流程
🎯 真实生产环境部署
🎯 成本控制和质量保证
🎯 可扩展的架构设计
```

### 技术亮点
```
✨ GitHub Actions 持续集成
✨ Webhook自动部署（腾讯云托管）
✨ CI门禁检查机制
✨ 腾讯云PaaS平台集成
✨ 真实API测试策略
✨ 零停机自动部署
```

### 商业价值
```
💼 企业级CI/CD实践
💼 自动化质量保证
💼 快速迭代和部署
💼 零运维成本（PaaS平台）
💼 高可用性和自动扩缩容
```

## 🔄 演示后操作

### 清理操作
```bash
# 停止演示用的本地服务
pkill -f "python main.py"

# 清理测试数据
rm -f test_*.log
rm -f *.pyc
rm -rf __pycache__

# 重置Git状态
git status
git clean -fd
```

### 后续步骤
```
📋 演示结束后：
1. 回答老师提问
2. 收集反馈意见
3. 讨论改进方向
4. 规划后续开发
```

---

**祝演示顺利！** 🚀

> 提示：演示时保持自信，重点突出自动化流程和实际应用价值。